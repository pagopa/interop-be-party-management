syntax = "proto2";

import "scalapb/scalapb.proto";

option (scalapb.options) = {
  package_name: "it.pagopa.pdnd.interop.uservice.partymanagement.model.persistence.serializer.v1"
};

message PartyRelationshipV1 {
  required string id = 1;
  required string from = 2;
  required string to = 3;
  required PartyRoleV1 role = 4;
  required string platformRole = 5;
  required string start = 6;
  optional string end = 7;
  required PartyRelationshipStatusV1 status = 8;
  optional string filePath = 9;

  enum PartyRelationshipStatusV1 {
    Pending = 0;
    Active = 1;
    Suspended = 2;
    Deleted = 3;
  }

  enum PartyRoleV1 {
    Delegate = 0;
    Manager = 1;
    Operator = 2;
  }
}

